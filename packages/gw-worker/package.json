{
  "name": "@gw1-mcp/gw-worker",
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev",
    "dev:node": "tsx src/node.ts",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run --outdir dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@gw1-mcp/gw-mcp": "workspace:*",
    "@hono/mcp": "^0.3.1",
    "hono": "^4.12.29"
  },
  "devDependencies": {
    "@hono/node-server": "^2.0.8",
    "wrangler": "^4.110.0"
  }
}